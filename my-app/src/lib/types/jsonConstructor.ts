// my-app/src/lib/types/jsonConstructor.ts
export function JsonConstructor(target: any, propertyKey: string, descriptor: PropertyDescriptor): void {
  // Placeholder implementation or leave empty if it's just a marker
  console.log("JsonConstructor decorator called on:", target, propertyKey);
}
